import { ContactEmailSummaryPipe } from './contact-email-summary.pipe';

describe('ContactEmailSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactEmailSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
