import { RecipientListPipe } from './recipient-list.pipe';

describe('RecipientListPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipientListPipe();
    expect(pipe).toBeTruthy();
  });
});
