import { ActionSummaryPipe } from './action-summary.pipe';

describe('ActionSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
