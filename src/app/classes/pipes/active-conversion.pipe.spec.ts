import { ActiveConversionPipe } from './active-conversion.pipe';

describe('ActiveConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
