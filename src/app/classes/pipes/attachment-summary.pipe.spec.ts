import { AttachmentSummaryPipe } from './attachment-summary.pipe';

describe('AttachmentSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new AttachmentSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
