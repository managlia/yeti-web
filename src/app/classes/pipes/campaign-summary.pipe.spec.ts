import { CampaignSummaryPipe } from './campaign-summary.pipe';

describe('CampaignSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new CampaignSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
