import { ContactSummaryPipe } from './contact-summary.pipe';

describe('ContactSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
