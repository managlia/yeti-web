import { CompanySummaryPipe } from './company-summary.pipe';

describe('CompanySummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanySummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
