import { MapToCommonIdPipe } from './map-to-common-id.pipe';

describe('MapToCommonIdPipe', () => {
  it('create an instance', () => {
    const pipe = new MapToCommonIdPipe();
    expect(pipe).toBeTruthy();
  });
});
